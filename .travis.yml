dist: xenial
language: python

install:
  - pip install -r requirements-test.txt
  - python setup.py install
script: python setup.py test

matrix:
    include:
      - name: "Python 3.5"
        dist: trusty
        python: "3.5"
      - name: "Python 3.6"
        dist: trusty
        python: "3.6"
      - name: "Python 3.7"
        python: "3.7"
      - name: "Lint"
        install: pip install flake8 flake8-bugbear flake8-comprehensions flake8-executable flake8-mypy flake8-pyi
        python:
          - "3.7"
        script: flake8
